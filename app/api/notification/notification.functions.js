'use strict';

var Notification = require('./notification.model.js'),
    async = require('async');

exports.create = function (params) {
    params.message
    var notification = {

    }
    Notification.create(notification, params.callback);
};
